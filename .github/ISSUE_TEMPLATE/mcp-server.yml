name: devcd/mcp-server
description: Work on read-only MCP resources and agent-facing context APIs.
title: "devcd/mcp-server: "
labels: ["devcd/mcp-server"]
body:
  - type: textarea
    id: integration
    attributes:
      label: Integration Goal
      description: Which MCP resource, prompt, or local context behavior is needed?
    validations:
      required: true

  - type: textarea
    id: boundaries
    attributes:
      label: Policy Boundaries
      description: What local-first, export, or action limits apply?
    validations:
      required: true